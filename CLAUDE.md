# 防災ハブ — プロジェクト設定

設計書: [docs/spec.md](docs/spec.md)

## セキュリティプロファイル

**選定: Tier 1 — Static（5項目）**

判定理由: 設計書に認証・DB保存・決済・外部API連携のいずれも無い。バックエンドを持たず、状態は `localStorage` に閉じる。外部への送信は一切行わず、全機能が公開情報へのリンク遷移のみで完結する。

リスクレベル: **低〜中**（実ユーザーが災害時に使う想定のため、可用性は重視するが、扱うデータは都道府県名のみで個人情報を保持しない）

### 適用項目と実装状況

| # | 項目 | 実装・確認方法 | 状態 |
|---|---|---|---|
| S-01 | .gitignore設定 | `.DS_Store` / `node_modules/` / `.env` / `.firebase/` | 済 |
| S-02 | ソースコード内シークレット検索 | `grep -rInE 'ghp_\|AKIA\|sk-\|AIzaSy\|password\s*[:=]' .` → 検出0件 | 済 |
| S-03 | GitLeaks | コミット時のpre-commitフックで実行 | 済 |
| S-04 | XSS基本対策 | 動的な文字列はすべて `textContent` で挿入。`innerHTML` はソース内に定義した自作SVGパス定数の代入1箇所のみ | 済 |
| S-05 | CDN SRI | 外部スクリプト・外部スタイル・外部フォント・外部画像を一切読み込まない（該当なし） | 済 |

### プロジェクト固有の追加項目

| # | 項目 | 理由 | 状態 |
|---|---|---|---|
| P-01 | 全外部リンクに `rel="noopener noreferrer"` | 遷移先からの `window.opener` 経由の書き換え（リバースタブナビング）を防ぐ。リンク集アプリなので外部遷移が主機能であり、通常より重要度が高い | 済（18リンク全件を実機で確認） |
| P-02 | CSPメタタグ | `default-src 'self'` を基点に、外部スクリプト読み込み・オブジェクト埋め込み・`base` タグ注入・フォーム送信をすべて禁止 | 済 |
| P-03 | `referrer` の抑制 | `<meta name="referrer" content="no-referrer">`。どのページから来たかを外部サイトに渡さない | 済 |
| P-04 | 全リンク先の疎通検証 | リンク集という性質上、死にリンクは機能停止に等しい。全URLのHTTPステータスを実測する | 済（設計書6-2に記録） |
| P-05 | Service Workerのキャッシュ汚染防止 | 自オリジンのGETのみ扱い、外部サイトのレスポンスは一切キャッシュしない。install時は `cache: 'reload'` でHTTPキャッシュを迂回し、古いHTMLを取り込まない | 済 |

## デザイン方針

**最優先要件: 老若男女・高齢者・操作に不慣れな人が直感的に使えること**（ユーザー明示指示）

UIは事前にGPT image 2でモックアップを生成し、ユーザー承認済み（`docs/mockup-top.png` / `docs/mockup-detail-region.png`）。以下はその承認済みモックアップを数値化したもの。

| 軸 | 判断 | 理由 |
|---|---|---|
| Typography | 本文17px / カード見出し19px / チップ18px、太さ800。OSの標準日本語フォントのみ | 外部フォントを読まない＝オフラインでも同じ見た目になる。災害時に読み込み待ちを発生させない |
| Color | 防災赤 `#C62828` を強調のみに使用。本文は `#16202C`（白背景に対しコントラスト比13:1超） | 赤を全面に使うと「どれが重要か」が消える。強調箇所を1つに絞ることで視線誘導が成立する |
| Motion | ホバー・トランジションは最小限、`prefers-reduced-motion` に対応 | 緊急時にアニメーションは邪魔になる。演出より即時性 |
| Spatial | タップ領域は最小88px（実測96〜139px）、カード間12px | 手が震えている・暗い・急いでいる状況を前提にする。一般的な44pxの倍を確保 |
| Texture | フラット。影は境界を示す最小限のみ | 装飾より視認性 |

**あえて崩した点**: 一般的なアプリはカテゴリをアイコングリッドで並べるが、本アプリは**縦1列の大きなカード**にした。グリッドは一覧性が高い反面、アイコンの意味を解釈する負荷がかかる。文字を大きく読ませる縦1列のほうが、高齢者にとって迷いが少ないと判断した。

## 運用ルール

- **無料枠のみ**。バックエンド・課金・カスタムドメインは使わない
- **リンク切れが最大のリスク**。年1回、全リンクの疎通確認を行う（設計書6-2にコマンド記載）
- 災害別URL（`r8kumamoto_jishin` 等）は絶対にハードコードしない。理由は設計書5章
