# 防災ハブ 設計書

最終更新: 2026-07-31

## 1. 概要

大規模災害時に「どのアプリ・どのページを見ればいいか」で迷わないようにするための、カテゴリ別リンク集約型のランチャーアプリ。

**このアプリ自体は防災データを持たない・生成しない。** 既存の信頼できる防災系サービス(NERV防災、Yahoo!防災速報、電力会社の停電情報、国の公的発表ページなど)への「入口」を整理して1箇所にまとめるだけの構成。これにより、外部データ取得・可用性・誤情報のリスクを避け、実装をシンプルに保つ。

きっかけ: 2026年7月28日発生の熊本県熊本地方 M7.1・最大震度7の地震(宇城市・氷川町)。

## 2. 対象ユーザー・UI方針

- **老若男女、高齢者や操作に不慣れな人でも直感的に使えること**が最重要要件
- 大きな文字・大きなタップ領域・高コントラスト・シンプルな線のアイコン・最小限のテキスト・広めの余白
- 写真調ではなくベクター調のフラットUI(装飾より視認性・タップしやすさを優先)

## 3. アプリ形態

- **PWA**(スマホのホーム画面に追加できる形)
- 単一HTMLファイル(HTML/CSS/JS内包) + `manifest.json` + Service Worker(オフラインでも画面自体は表示できるようキャッシュ)
- バックエンドなし。地域選択などの状態は `localStorage` に保存し、次回起動時に自動反映
- ホスティングは GitHub Pages(無料枠)

## 4. 画面構成

### 4-1. トップ画面
- ヘッダー: アプリ名「防災ハブ」(左) + 現在地域表示(右、例:「熊本県」)
- 災害種別トグル(手動選択、4つの大きなチップ): 「地震」「大雨・洪水」「停電」「断水」
  - タップした災害種別に対応するカテゴリカードが**一番上に移動し、赤枠で強調表示**される
  - 外部データ取得は行わない(利用者が今の状況を自分で選ぶだけ。オフラインでも確実に動作する)
- カテゴリカード一覧(下記6.のカテゴリ。アイコン+タイトル+短いサブタイトル+タップで詳細画面へ)
- フッター: 「地域を変更」ボタン

### 4-2. カテゴリ詳細画面
- 戻る矢印 + カテゴリ名
- 「開くアプリを選んでください」
- カテゴリ内の子アプリ/リンクを大きなカードで縦に並べる。タップで該当の公式URLへ遷移(7.の仕組み参照)

### 4-3. 地域を変更画面
- 戻る矢印 + 「地域を変更」
- 都道府県検索ボックス + 都道府県リスト(選択中の県は赤背景+チェックマーク)
- 「決定」ボタンで確定 → `localStorage` に保存 → 以後トップ画面・各リンクに自動反映

参考画像: `docs/mockup-top.png`(トップ画面) / `docs/mockup-detail-region.png`(①カテゴリ詳細画面 ②地域変更画面)

## 5. ★リンク設計の最重要原則: 恒久リンクと災害別リンクの区別

**このアプリの寿命を左右する設計判断。** 公的機関の災害情報ページには2種類ある。

| 種類 | 例 | 扱い |
|---|---|---|
| **恒久リンク**(常設の入口。常に最新の災害が最上部に並ぶ) | 内閣府防災「災害情報一覧」`bousai.go.jp/updates/`、国交省「災害・防災情報」`mlit.go.jp/saigai/` | **これをアプリに埋め込む。** 次の災害が起きても自動で最新に追従する |
| **災害別リンク**(その災害専用に作られたURL) | 内閣府 `updates/r8kumamoto_jishin/`、経産省 `meti.go.jp/2026_kumamoto/` | **埋め込まない。** 災害ごとにURLが変わるため、次の災害では死にリンクになる |
| **市区町村の生活支援情報**(給水・物資配布) | 熊本市の給水拠点情報など | **埋め込まない。** 全国1,700以上あり維持不能。地域名を使った検索リンクで対応 |

→ 原則: **「常に最新が出てくる入口」だけをハードコードする。** これにより、アプリを更新しなくても次の災害で機能し続ける。

### 5-1. ★検索エンジンへ飛ばすリンクは置かない（2026-07-31 利用者指摘により全廃）

当初、市区町村レベルの情報は「地域名＋キーワード」のGoogle検索リンクで代替していた。これは**誤りだった。**

> 「ただのGoogleサイトに移動するのはやめてください。それだとこのアプリを使う意味がなくなり最初からGoogle検索した方が早いです」（利用者指摘）

検索結果ページを挟むと、利用者は結局そこから自分で選ぶことになり、ランチャーとしての価値が消える。**リンクは必ず目的のページそのものを開くこと。**

代わりに採用した仕組み:

| 情報 | 直リンク先 | 粒度 |
|---|---|---|
| 避難情報・避難所の開設 | Yahoo!天気・災害 `crisis.yahoo.co.jp/evacuation/{都道府県JIS2桁}/{市区町村JIS5桁}/` | **市区町村** |
| 避難場所マップ | Yahoo!天気・災害 `crisis.yahoo.co.jp/map/?pref={JIS2桁}` | 都道府県 |
| 給水・物資・生活支援 | 都道府県の防災ポータル（23件）／無い県は都道府県公式サイト（47件） | 都道府県 |
| 停電 | 電力会社10社（6-2参照） | 都道府県 |

市区町村データは総務省「全国地方公共団体コード」から生成した全国1,918件。団体コード6桁の先頭5桁がYahooのURLのJISコードと一致する（例: 宇土市 432113 → 43211）。

### 5-2. ★リンク先ドメインは官公庁ドメインに限定する（安全上の必須要件）

都道府県の防災ポータルを探索した際、**民間が取得した紛らわしいドメインが多数見つかった**:

| ドメイン | 実体 |
|---|---|
| `bousai.okinawa.jp` | 詐欺返金支援センター（沖縄県とは無関係） |
| `aichi-bousai.jp` | 民間の消防設備会社 |
| `bousai.tokyo.jp` | 個人系ブログ |
| `nagano-bousai.jp` | 長野**市**（県ではない） |

災害時にこれらへ誘導すれば実害が出る。**採用してよいのは `*.pref.*.jp` / `*.lg.jp` / `*.metro.tokyo.lg.jp` などの官公庁ドメインのみ。** `<県名>-bousai.jp` 形式のドメインは、名前が正しそうに見えても採用しない。リンクを追加する際は必ずこの制約を守り、実際にアクセスして中身を確認すること。

### 5-1-2. ★カテゴリ名と中身を一致させる（2026-07-31 利用者指摘）

「給水・食料の配布」を開いたのに避難情報しか出てこない、という指摘を受けた。原因は、**県の防災ポータルに給水情報があるはずだと確認せずに書いたこと**。実際に「防災情報くまもと」を開いて全文を調べたところ、「給水」「断水」「物資」「食料」の語が**1つも無かった**（掲載内容は避難情報・気象・地震・通行規制・河川水位のみ）。

調査の結果、**給水所・物資配布の情報は市区町村が出している**ことが確認できた:

| 自治体 | ページ |
|---|---|
| 宇土市 | 「今回の地震に伴う断水について」`city.uto.lg.jp` |
| 宇城市 | 「地震に伴う断水及び給水のお知らせ」`city.uki.kumamoto.jp` |
| 熊本市 | 「義援物資について」`city.kumamoto.jp` |

そこで **全国1,913市区町村の公式サイトURL** を組み込み、給水カテゴリの先頭を市区町村公式サイトにした。URLはJ-LIS（地方公共団体情報システム機構）「全国自治体マップ検索」から取得している（推測で組み立てていない）。

**教訓: リンクの説明文に「〜の情報もここに出ます」と書くときは、必ずそのページを開いて実際に載っているか確認する。**

### 5-3. 採用を見送ったもの

- **Yahoo!「災害マップ（給水所など）」**: 給水所が載る有用なマップだが、URLが `?lat=32.59&lon=130.69` 固定で、47都道府県すべてのページで同じ座標（＝現在発生中の災害の座標）を返した。つまり**災害別リンク**であり、埋め込むと次の災害で誤った場所を指す。市区町村の避難情報ページから1タップで辿れるため、アプリ側では持たない
- **NHK の都道府県別ページ** `nhk.or.jp/kishou-saigai/pref/{code}/`: 実際に開くと汎用の天気ページへリダイレクトされ、都道府県別にならない
- **電力会社の市区町村別 停電情報**: 「特定の地域が停電中か・復旧したかを見たい」という要望を受けて調査したが、**固定URLでは実現できない**。九州電力送配電・東京電力パワーグリッドの両方を実機で確認したところ、市区町村レベルの絞り込みは**停電が発生している間だけリンクが有効になる**仕組みで（停電0件のときは `teiden_link_nashi` クラスが付いてhrefが消える）、平常時にアクセスできる恒久URLが存在しない。したがって各社サイト内で地図を辿ってもらう形になる
  - ただし東京電力パワーグリッドだけは `teideninfo.tepco.co.jp/flash/{都道府県JIS2桁}000000000.html` という都道府県別の恒久URLがあるため、管内8県はこちらへ直接遷移するようにした

## 6. カテゴリ構成(確定)

| # | カテゴリ | 内容 | リンク方式 |
|---|---|---|---|
| 1 | 地震・気象速報 | NERV防災 / Yahoo!防災速報 / NHKニュース・防災 / 気象庁 | 全国共通の恒久リンク |
| 2 | 避難所・避難情報 | 選択都道府県の避難所開設情報 | 「選択地域名 + 避難所 開設」の検索リンク |
| 3 | 停電情報 | 選択都道府県に対応する電力会社の停電情報ページ(例: 熊本県→九州電力送配電) | 全国10電力会社の恒久リンクに、都道府県→電力会社の対応表でマッピング(下記6-2の実測URL) |
| 4 | 給水・食料配布 | 選択都道府県の給水拠点・物資配布情報 | 「選択地域名 + 給水 支援物資」の検索リンク |
| 5 | **支援状況・国の対応**(新規) | 内閣府防災「災害情報一覧」/ 国交省「災害・防災情報」 | 恒久リンク(5.参照) |
| 6 | 通信手段(ネットが使えない時) | 00000JAPAN(災害時無料Wi-Fi) / 災害用伝言板web171 / 災害用伝言ダイヤル171 | 全国共通の恒久リンク。**災害種別に関わらず常時表示** |
| 7 | 平常時の備え | 農林水産省「家庭備蓄ポータル」 | 恒久リンク。災害が起きていない時に見る枠として下部に配置 |

### 6-1. カテゴリ5「支援状況・国の対応」の調査結果(2026-07-31 実ページ取得により確認)

利用者から「支援の状況が分かる、随時更新されるリンクを入れたい。農水省が発信しているのか？」という要望を受けて実際に各省庁のページを取得して確認した結果:

- **農林水産省**: 災害情報ページの中身は、被災した農林漁業者向けの**融資・共済・償還猶予**が中心。一般の被災者向けに「今どこで食料が配られているか」といったリアルタイムの支援状況は発信していない。ただし平常時向けの「家庭備蓄ポータル」は有用なので、カテゴリ7として別枠で採用する
- **内閣府(防災担当)**: **ここが本命。** 国が被災自治体の要請を待たずに物資を送る「プッシュ型支援」の実施状況を含む被害状況等を公表している。かつ「災害情報一覧」が恒久リンクとして機能し、最新の災害が常に最上部に並ぶ
- **国土交通省**: 「災害・防災情報」が恒久的なポータルとして機能。道路の被害状況マップ、鉄道各社・空港・航空・旅客船の運行状況、TEC-FORCE(緊急災害対策派遣隊)の派遣状況など、**移動・物流に関する情報**が集約されている
- **都道府県公式ページ**: 熊本県の令和8年熊本地震ページを確認したところ、掲載の中心は相談窓口・義援金受付・健康注意喚起であり、給水拠点や物資配布のリアルタイム情報は含まれていなかった。**給水・物資の実務情報は市区町村レベル**(熊本市など)にあるため、カテゴリ4は検索リンク方式が妥当であることが裏付けられた

### 6-2. 電力会社10社の停電情報URL(2026-07-31 実測により確定)

実装時に全社のURLを curl で検証したところ、**当初この設計書に書いていた推測URLは10社中7社が404**だった。以下は実際にHTTP 200を確認した確定版。

| 電力会社 | 停電情報URL | 担当都道府県数 |
|---|---|---|
| 北海道電力ネットワーク | `https://teiden-info.hepco.co.jp/` | 1 |
| 東北電力ネットワーク | `https://nw.tohoku-epco.co.jp/teideninfo/` | 7 |
| 東京電力パワーグリッド | `https://teideninfo.tepco.co.jp/` | 8 |
| 中部電力パワーグリッド | `https://teiden.powergrid.chuden.co.jp/p/index.html` | 5 |
| 北陸電力送配電 | `https://www.rikuden.co.jp/nw/teiden/otj010.html` | 3 |
| 関西電力送配電 | `https://www.kansai-td.co.jp/teiden-info/index.php` | 6 |
| 中国電力ネットワーク | `https://www.teideninfo.energia.co.jp/` | 5 |
| 四国電力送配電 | `https://www.yonden.co.jp/nw/safety/teiden/` | 4 |
| 九州電力送配電 | `https://www.kyuden.co.jp/td_teiden/kyushu.html` | 7 |
| 沖縄電力 | `https://www.okiden.co.jp/livelihood/teiden/teiden-info/` | 1 |

合計47都道府県すべてを網羅（実装後にコード上でも未マッピング0件を確認済み）。

**運用上の注意**: 中部電力は `teiden.chuden.jp` がDNSごと廃止され `teiden.powergrid.chuden.co.jp` に移転していた。電力会社の停電情報URLは**恒久リンクの中でも移転しやすい部類**なので、年1回程度は全社の疎通確認を行うこと。検証コマンド:

```
curl -sS -o /dev/null -w '%{http_code} %{url_effective}\n' -L <URL>
```

**県境の扱い**: 静岡県東部（東京電力管内）、福井県嶺南（関西電力管内）のように、県内でも管轄が分かれる地域がある。都道府県単位のマッピングでは拾えないため、停電カテゴリには検索リンクを併置し、画面上にも注意文を表示している。

### 6-3. 全リンクの実測検証（2026-07-31実施）

**リンク集アプリなので、死にリンクは機能停止に等しい。** 全件を実際にアクセスして検証した。

| 対象 | 件数 | 結果 |
|---|---|---|
| 市区町村の公式サイト | 1,913 | **1,911が200。** 残り2件（富岡市・舟橋村）はPythonのHTTPライブラリでのみ失敗し、curlと実ブラウザでは正常動作を確認 |
| 都道府県レベルのリンク（公式47・防災ポータル23・電力10・全国共通14） | 94 | **全件200** |
| 市区町村の避難情報URL（Yahoo!天気・災害） | 1,918 | JISコードから機械的に生成。代表33件の実在を実測確認 |

**初回検証では46件が不通だった。** 原因は3つ:

1. **`http` を `https` に強制変換したこと**（30件超）。J-LIS掲載どおりの `http` に戻して解決。現在も27件は `http` のままが正しい
2. **J-LIS掲載URLが深いパスで、そのページが消えていた**（神戸市9区・白馬村・厚沢部町）。ドメインのトップに差し替え
3. **自治体のドメイン移転**（八千代市・岸和田市・熊野町・大桑村・高森町・吉岡町）。検索と実測で新URLを特定して差し替え

**教訓: 取得したURLをそのまま信じない。スキームの書き換えは特に危険**（良かれと思ったhttps化が30件超の死にリンクを生んだ）。

**検証時の注意**: 全1,913件を検証しても、1サーバーあたり1リクエストなので負荷は集中しない。一方、Yahoo!のように**同一サーバーへ大量に投げると429が返る**（実際に発生）。同一ホストへの連続アクセスは代表サンプルに留めること。

## 7. 外部アプリへの遷移の仕組み

各カードは対象サービスの**通常のhttps公式URL**にリンクするだけで、防災ハブ側で特別なディープリンク実装は行わない。

- **インストール済みの場合**: iOS/AndroidのOS標準機能(ユニバーサルリンク/アプリリンク)により、対応済みアプリがあればOSが自動判定し、ブラウザを経由せずアプリが直接開く
- **未インストールの場合**: 通常のWebページが開く(多くの場合そこにアプリストアへの誘導が表示される)
- この挙動は各サービス側の実装に依存する(防災ハブ側でコントロール不可)。ただし主要アプリ(NERV防災・Yahoo!防災速報・NHK等)は概ね対応している

## 8. 災害種別ハイライトの仕組み(確定: 手動選択方式)

- 利用者がトグルで今の状況(地震/大雨・洪水/停電/断水)を選ぶと、対応カテゴリが上部に移動+強調表示される
- 気象庁等の外部データ取得は行わない(自動検知方式は不採用。理由: CORS対応・取得失敗時のフォールバックなど複雑さが増し、単一HTML+GitHub Pagesという軽量構成から外れるため)
- カテゴリ6(通信手段)は災害種別に関係なく常に表示する

## 9. 地域データ

- 都道府県選択(47都道府県)を `localStorage` に保存
- 都道府県 → 電力会社(10社)の対応表を静的データとして保持
- 都道府県名は避難所・給水カテゴリの検索リンク生成にも使用

## 10. セキュリティ・運用方針

- 無料枠のみ(GitHub Pages)。バックエンドなし、ユーザーデータの収集・送信は行わない(全てブラウザのlocalStorageに閉じる)
- GitHubリポジトリはPrivateで作成
- シークレット・APIキー等は使用しない(全て公開情報へのリンクのみのため)
- **00000JAPANの注意書きをアプリ内に明記する**: 緊急時の接続性を優先して暗号化されていないため、ID・パスワード等の重要な通信は避ける旨を、カテゴリ6の画面に短い注意文として表示する

## 11. 実装ステップ

1. GitHubリポジトリ作成(本設計書を格納) — 完了
2. PWA本体のコーディング(単一HTML + manifest.json + service worker)
3. Playwrightでの実機確認(スマホ幅・ホーム画面追加・地域切替保存・災害種別トグル動作)
4. GitHub Pagesへデプロイ
5. コミット&プッシュ、完了報告(公開URL)
